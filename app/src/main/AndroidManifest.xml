<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.rycry.operacionesbasicas">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OperacionesBasicas"
        tools:targetApi="31">

        <!-- Actividad principal (Menú) -->
        <activity
            android:name=".MainActivityMenu"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Otras actividades -->
        <activity
            android:name=".MainActivityDividir"
            android:exported="false" />
        <activity
            android:name=".MainActivityRestar"
            android:exported="false"
            android:theme="@style/Theme.OperacionesBasicas" />
        <activity
            android:name=".MainActivitySumar"
            android:exported="false" />
        <activity
            android:name=".MainActivityMultiplicar"
            android:exported="false" />
    </application>

</manifest>
